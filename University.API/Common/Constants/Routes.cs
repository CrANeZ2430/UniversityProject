namespace University.API.Common.Constants;

public static class Routes
{
    private const string _prefix = "api";

    public const string Faculties = $"{_prefix}/faculties";
    public const string Departments = $"{_prefix}/departments";
    public const string Groups = $"{_prefix}/groups";
    public const string Students = $"{_prefix}/students";
}
